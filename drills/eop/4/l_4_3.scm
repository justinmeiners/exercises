; LEMMA
; the symmetric complement of a weak ordering is an equivalnce relation
; P(a, b) = ~r(a, b) and ~r(b, a)

; PROOF
; 1. Reflexive:
;   Suppose a = a
;   Then under the equivalence relation e(a, a)
;   so ~r(a, a) and ~r(a, a)
;   so P(a, a)

; 2. Symmetric
;   Suppose P(a, b)
;   then ~r(a, b) and ~r(b, a)
;   so ~r(b, a) and ~r(a, b)
;   so P(b, a)

; 3. Transitive
;   Suppose P(a, b) and P(b, c)
;   then [~r(a, b) and ~r(b, a)] and [~r(b, c) and ~r(c, b)]
;   so under the equivalence e(a, b) and e(b, c)
;   so e(a, c)
;   so ~r(a, c) and ~r(c, a)
;   so P(a, c)



