; EXCERCISE
; Give an example of two polynomials with integer coffecients
; for which the algorithm does not terminate. (lexographically ordered)


; a(x) = x^2 + x
; b(x) = 1

; So a(x) > b(x)

; a(x) - nb(x)  = x^2 + x - n > b(x)

; so the algorithm will not terminate

