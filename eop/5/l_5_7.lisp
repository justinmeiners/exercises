; LEMMA
; In an ordered additive group
; a < b => -b < -a

; PROOF
; Suppose a < b.
; a + (-a) < b + (-a)       (add -a)
; 0 < b - a                 (cancel a)
; 0 + (-b) < (b - a) + (-b) (add -b)
; so 
; -b < -a

