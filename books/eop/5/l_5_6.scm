; LEMMA
; In an ordered additive monoid viewed as a semi-module over the natural numbers

; a > 0 and n > 0 => na > 0

; PROOF
; Suppose n = 1, then a > 0
; assume that a + a + ... + a = na > 0
; then (a + a + .. + a) + a = na + a > a > 0
; = (n+1)a > 0

